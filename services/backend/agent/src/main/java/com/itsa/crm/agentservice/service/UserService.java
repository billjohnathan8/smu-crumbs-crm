package com.itsa.crm.agentservice.service;

import com.itsa.crm.agentservice.dto.UserDto;
import java.util.List;
import org.springframework.stereotype.Service;

/**
 * Placeholder user service for future domain logic.
 */
@Service
public class UserService {
    /**
     * Returns a list of users (currently empty).
     *
     * @return empty list until implemented
     */
    public List<UserDto> listUsers() {
        return List.of();
    }
}
