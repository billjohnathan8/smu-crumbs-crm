spring:
  datasource:
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}

app:
  verification-email:
    provider: ${VERIFICATION_EMAIL_PROVIDER:mock}
    dispatch:
      enabled: ${VERIFICATION_EMAIL_DISPATCH_ENABLED:true}
  jwt:
    hmac-secret: ${JWT_HMAC_SECRET:dev-only-insecure-secret}
