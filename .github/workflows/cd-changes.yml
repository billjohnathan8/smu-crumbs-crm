name: "[DEPRECATED] CD - Changes Deploy"
# ┌──────────────────────────────────────────────────────────────────┐
# │  DEPRECATED — DO NOT USE                                        │
# │  Replaced by:                                                   │
# │    - cd-main.yml for CI-gated mainline deployments              │
# │    - cd-force-spec.yml for manual selective deployments         │
# │    - cd-force-all.yml for manual full deployments               │
# │                                                                  │
# │  workflow_dispatch removed so this no longer appears in the     │
# │  Actions sidebar. File kept for git history reference only.     │
# └──────────────────────────────────────────────────────────────────┘

on:
  workflow_call:

permissions:
  contents: read

jobs:
  deprecated:
    name: This workflow is deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "::error::This workflow is deprecated. Use cd-main.yml, cd-force-spec.yml, or cd-force-all.yml instead."
          exit 1
