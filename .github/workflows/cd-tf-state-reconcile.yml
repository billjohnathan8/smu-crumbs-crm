name: "[DEPRECATED] CD - Terraform State Reconcile"
# ┌──────────────────────────────────────────────────────────────────┐
# │  DEPRECATED — DO NOT USE                                        │
# │  Reconciliation is now automatic in:                             │
# │    - cd-tf-infra-plan.yml (during plan)                          │
# │    - cd-tf-infra-provision.yml (during apply)                    │
# │                                                                  │
# │  workflow_dispatch removed so this no longer appears in the      │
# │  Actions sidebar. File kept for git history reference only.      │
# └──────────────────────────────────────────────────────────────────┘

on:
  workflow_call:

permissions:
  contents: read

jobs:
  deprecated:
    name: This workflow is deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "::error::This workflow is deprecated. Reconciliation is now automatic in cd-tf-infra-plan.yml and cd-tf-infra-provision.yml."
          exit 1
