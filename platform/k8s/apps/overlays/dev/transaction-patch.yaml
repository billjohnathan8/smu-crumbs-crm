apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction
  namespace: dev
spec:
  template:
    spec:
      containers:
        - name: transaction
          env:
            - name: CLIENT_SERVICE_URL
              value: http://client.dev.svc.cluster.local

