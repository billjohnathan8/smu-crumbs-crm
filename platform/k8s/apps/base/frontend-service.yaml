apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: crm
  labels:
    app: frontend
    component: ui
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: frontend
